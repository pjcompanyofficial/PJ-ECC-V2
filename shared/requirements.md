## Packages
qrcode.react | For generating secure QR codes for employees
html5-qrcode | For camera-based QR code scanning
framer-motion | For holographic animations and page transitions

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
colors: {
  pj: {
    orange: "hsl(var(--pj-orange))",
    red: "hsl(var(--pj-red))",
    green: "hsl(var(--pj-green))",
    dark: "hsl(var(--pj-dark))",
    card: "hsl(var(--pj-card))",
  }
}
